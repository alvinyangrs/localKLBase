apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: standalone-claim0
  name: standalone-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
